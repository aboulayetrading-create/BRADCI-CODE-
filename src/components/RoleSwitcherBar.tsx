import React from 'react';
import { useApp } from '../context/AppContext';
import { 
  ShoppingBag, 
  Store, 
  Bike, 
  ShieldCheck, 
  MapPin, 
  UserCheck, 
  Building2,
  Sparkles,
  ChevronRight
} from 'lucide-react';

export const RoleSwitcherBar: React.FC = () => {
  // Supprimé suite à la demande de l'utilisateur (barre de rôle démo retirée des enchères)
  return null;
};
